# Web UI

This directory is a placeholder for the Next.js admin dashboard.

The Web UI is a separate project that calls the Admin API exposed by the Go gateway at `/admin/*`.

## Setup

```bash
npx create-next-app@latest . --typescript --tailwind --app
```
